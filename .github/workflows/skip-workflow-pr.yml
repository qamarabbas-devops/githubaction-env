name: Skip Workflow - PR

on:
    pull_request:
        branches: [ main ]

jobs:
     build:
        name: Build
        runs-on: ubuntu-latest
        
        steps:
            - name: Build The Code
              run: |
                echo "Build The Code"